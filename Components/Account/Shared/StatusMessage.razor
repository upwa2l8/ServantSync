@if (!string.IsNullOrEmpty(Message))
{
    <MudAlert Severity="@(IsError ? Severity.Error : Severity.Success)" Dense="true" Class="mb-3">
        @Message
    </MudAlert>
}

@code {
    [Parameter] public string? Message { get; set; }
    [Parameter] public bool IsError { get; set; }
}
